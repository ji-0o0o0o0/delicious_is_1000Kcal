# 🛠 Engineering Decisions & 트러블슈팅

## Engineering Decisions (핵심 고민과 선택)

### 1️⃣ 데이터 수집: OCR vs 크롤링

카카오톡의 폐쇄적인 API 정책상 직접적인 크롤링이 불가능했습니다.

대안으로 Tesseract OCR을 선택했으며,
인식률을 높이기 위해 특정 키워드(`운성`, `식성` 등) 기반의 파싱 로직을 직접 설계하여 데이터 정확도를 확보했습니다.

---

### 2️⃣ 인프라: Google Sheets API vs 자체 DB/웹

초기에는 웹 서비스 구축을 고려했으나,
모임 멤버들의 접근성과 별도의 앱 설치 없는 실시간 공유를 위해 Google Sheets를 데이터 저장소 겸 대시보드로 활용했습니다.

- 서버 비용 제로화
- 별도 배포 불필요
- 실시간 협업 가능

---

### 3️⃣ 운영 효율: Jar 외부 설정 파일 분리

스케줄러 주기나 멤버 명단 변경 시 매번 재빌드 및 재배포하는 비효율을 방지하기 위해
`config.properties` 와 `members.txt` 를 jar 외부에 분리하여 런타임에 동적으로 로드하도록 설계했습니다.

- 설정 변경 시 재배포 불필요
- 운영 편의성 향상

---

### 4️⃣ 장애 대응: OCR 정정 Feedback Loop

OCR 인식 오류나 사용자 오타에 대응하기 위해,
구글 시트에서 수동 수정 후 특정 플래그(`Y`)를 입력하면:

1. 시스템이 수정 여부 감지
2. 통계 자동 재계산
3. 완료 후 `D` 상태 변경

이 이루어지는 자기 수정형 로직을 구현했습니다.

---

### 5️⃣ 주간 통계 업데이트 정책

월요일에 한 번 확정하는 방식과 실시간 업데이트 방식을 분리하여 운영했습니다.

**이번주현황**
- 매일 업데이트
- 실시간 진행 상황 확인 목적

**주간통계**
- 매주 월요일 자동 생성
- 지난주 확정 결과 히스토리 관리 목적

---

### 6️⃣ UI 추가 이유

매번 터미널에서 실행하는 불편함을 줄이기 위해 Swing UI를 추가했습니다.

- Ctrl+V 이미지 붙여넣기 → 자동 저장 + OCR 실행
- 스케줄러 재시작
- 새 jar 적용용 재실행

---

## 🔧 트러블슈팅

### cmd 창이 남아있는 문제

**문제**: 종료 버튼 클릭 후 bat 파일 재실행 시 cmd 창이 화면에 남아있는 현상 발생.

**원인**: `Runtime.exec("cmd /c start ...")` 의 `start` 명령어가 새 cmd 창을 열어서 실행하는 방식이라 창이 남음.

**해결**: `ProcessBuilder` 방식으로 변경하여 cmd 창 없이 백그라운드에서 직접 bat 파일 실행.

```java
// 기존
Runtime.getRuntime().exec("cmd /c start \"" + startupPath + "\"");

// 변경
ProcessBuilder pb = new ProcessBuilder("cmd", "/c", startupPath);
pb.start();
```

---

### OCR 인식률 최적화: 폰트보다 중요한 데이터 밀도

**문제**: 초기 테스트 시 특정 메시지의 한글 인식률이 낮아 데이터 정합성이 깨지는 현상 발생.

**가설 및 검증**:
- 폰트 문제: 맑은 고딕 → 나눔고딕 ExtraBold → 굴림 으로 변경했으나 유의미한 개선 없음
- 크기 문제: 이미지 내 텍스트 크기를 100% → 125%로 확대하여 재테스트

**결과**: 텍스트 크기를 키워 OCR 엔진이 분석할 수 있는 픽셀 정보량을 늘림으로써 인식률을 95% 이상으로 개선.

**배운 점**: 기술(Tesseract) 자체의 한계보다 입력 데이터의 품질(전처리)이 결과에 더 큰 영향을 미친다는 것을 체감함.